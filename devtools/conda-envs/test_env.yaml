name: test
channels:

  - conda-forge

  - defaults
dependencies:
    # Base depends
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov

  - numpy
  - ase
  - dscribe
  - pymatgen
  - scikit-learn
  - onnxruntime

    # Pip-only installs
  #- pip:
  #  - codecov

